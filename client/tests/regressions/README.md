# Client Regression Tests

Put permanent Electron, browser-state, and UI logic regression tests here when
fixing client bugs.

Each test should:

- Reproduce the old failure mode as directly as possible.
- Assert the behavior that must never regress.
- Include the issue or PR number in the test name or a short comment.

Use names like `issue-1234-terminal-restore.test.mjs`.
