// Permanent incident coverage: swap-thrash desktop freezes (#181). The shared
// admission suite verifies atomic reservations and parity for UI, control, and
// restore decisions under projected memory pressure.
import "../memory-guard.test.mjs";
